<h3>Welcome</h3>
<div class="user_playlists">

<h3><%= @user.username %>'s recent playlists</br></h3>
<% @playlists.each do |playlist| %>
<%= link_to playlist.title, playlist_path(playlist) %></br>
<% end %>
<div class="nearby_playlists">
  <h3>Nearby playlists</h3>
</div>
<%= link_to "Create A New Playlist", new_playlist_path %>
