<h1>Tag a location</h1>
<%= form_for [@playlist, @location] do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :address %>
	<%= f.text_field :address %>
	
	<div class="link_tabs">
		<%= f.submit "Tag", class: "button" %>
	</div>
<% end %>
